cifar10: {
  openml_id: 40927,
  preprocessor: {
    principal_resnet_components: {
      n_components: 32,
      size: 32
    }
  },
  filters: {
    binarization: {
      label_zero: "1",
      label_one: "9"
    }
  },
  train_size: 500,
  dev_size: 500,
  test_size: 2000
}